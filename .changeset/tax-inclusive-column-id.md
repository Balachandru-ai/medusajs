---
"@medusajs/dashboard": patch
---

Fix store add-currencies form column id: use `is_tax_inclusive` instead of `select` for the tax inclusive pricing display column to avoid id collisions with row selection.
